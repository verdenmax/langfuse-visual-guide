# M4 Spec — Part 4 · 查询链路 / The read / query path (L20–L27)

**Date:** 2026-06-27 · **Milestone:** M4 · **Module:** `src/part4.py` (new; `registry.py` imports it)
**Langfuse source:** `/home/verden/course/langfuse`
**Depends on:** M2 (storage), M3 (ingestion/write path). Seeds:
`session files/explore-query-web.md` (12-topic read-path map, opus explore).

## Goal
Author L20–L27 in `src/part4.py`. Same bar as M1–M3: bilingual, ≥1800 zh CJK, ≥2 `.fig` SVGs/lang,
≥3 HTML visual blocks/lang, analogy + key-points + spark cards, real `path:line` citations.
This is the **read path** — the mirror of M3's write path. Recurring themes to thread:
(a) one `FilterState`/`ColumnDefinition` abstraction → many SQL backends; (b) compact list vs
full/metrics fetch; (c) time-bounded CH scans + look-back windows + conditional `FINAL`;
(d) zod-verified, parameterized inputs everywhere; (e) URL-as-source-of-truth for table state.

## Verified source anchors (to spot-check against the real repo while authoring each lesson)

- **L20 web 应用架构**: `web/src/pages/**` (Pages Router primary: `pages/project/[projectId]/...`,
  `pages/api/**`); `web/src/app/*` (only 4 files: layout + 3 route handlers — stripe webhook,
  chatCompletion, in-app-agent). Three API styles: tRPC (`pages/api/trpc/[trpc].ts`), REST
  (`pages/api/public/**`), App-Router/SSE (`pages/api/dashboard/execute-query-stream.ts`).
  `web/src` dirs: `features/`, `components/`, `server/`, `hooks/`, `ee/`.
- **L21 tRPC 骨架**: `web/src/server/api/root.ts:70` (`appRouter = createTRPCRouter({...})`, ~64
  routers; `AppRouter` type :136). `web/src/server/api/trpc.ts`: `createTRPCContext` :57 →
  `{session, headers, prisma}`; init superjson+Zod :103-124; `withErrorHandling` :175
  (ClickHouseResourceError → UNPROCESSABLE_CONTENT); `withOtelInstrumentation` :218; procedures
  `publicProcedure` :249, `authenticatedProcedure` :272, `protectedProjectProcedure` :379
  (`enforceUserIsAuthedAndProjectMember` :288, parses projectId from getRawInput before handler),
  `protectedOrganizationProcedure` :464, `protectedGetTraceProcedure` (`enforceTraceAccess` :483,
  allows public traces). Gates `requireFeatureFlag` :384.
- **L22 仓储层（从 ClickHouse 读）**: `packages/shared/src/server/repositories/clickhouse.ts`:
  `queryClickhouse<T>()` :639 (asserts no legacy-events read, OTel span, exp-backoff retry
  :649-694, resource-error wrap :696); `sendClickhouseQuery` :580 (sets
  `clickhouse_settings.log_comment = JSON(tags)` :600 → every query tagged with project/surface/
  route); streaming `queryClickhouseStream` :249, `queryClickhouseWithProgress` :705; raw export
  `queryClickhouseExecRaw` :405; `ClickHouseResourceError` :58. Look-back windows
  (`repositories/README.md`): `OBSERVATIONS_TO_TRACE_INTERVAL`,
  `SCORE_TO_TRACE_OBSERVATIONS_INTERVAL` injected into WHERE to bound scans.
- **L23 过滤·搜索栏·查询构建**: ClickHouse stack
  `packages/shared/src/server/queries/clickhouse-sql/`: `clickhouse-filter.ts:20` (Filter classes
  StringFilter:36 etc., `apply()` → `{query, params}` random param names, injection-safe);
  `factory.ts:37` `createFilterFromFilterState` (verifies column vs CH schema,
  `getProjectIdDefaultFilter` :217 mandatory project filter); `orderby-factory.ts`; `search.ts`
  (`clickhouseSearchCondition` ILIKE). Postgres stack `filterToPrisma.ts:25`,
  `orderByToPrisma.ts:15`. Search-bar `web/src/features/search-bar/` (README; URL FilterState as
  single source of truth; `lib/filter-state-to-query.ts`; AI filters `server/router.ts:45`
  round-trips model output through `filterStateToQueryText` to drop hallucinated columns).
- **L24 列表与表格**: `traces-ui-table-service.ts:206` `getTracesTableGeneric` (select
  count/rows/metrics/identifiers; propagates trace-id+timestamp into scores/observations CTEs;
  conditional joins `requiresScoresJoin`/`requiresObservationsJoin`; FINAL tradeoff :443-455 —
  default `toDate(timestamp),event_ts DESC` + `LIMIT 1 BY id` no FINAL, non-default → `FROM traces t
  FINAL`). Frontend `components/table/utils/joinTableCoreAndMetrics.ts:1` (core+metrics merge by
  id, paints before metrics resolve); table-view-presets (URL viewId + session + DB);
  global-time-range (Zustand ⊕ URL → mandatory timestamp filter).
- **L25 trace 详情与观测树**: `web/src/server/api/routers/traces.ts`: `byId` :348 (ctx.trace
  prefetched), `byIdWithObservationsAndScores` :368 (parallel getObservationsForTrace +
  getScoresAndCorrectionsForTraces, derives latency, partitions CORRECTION scores); verbosity
  `compact|truncated|full` (trpc.ts:480); agent graph `getAgentGraphData` :610 →
  `events.ts:getAgentGraphDataFromEventsTable`; client `web/src/features/trace-graph-view/`.
  Observation IO lazy-loaded per node (not in tree call).
- **L26 sessions**: `web/src/server/api/routers/sessions.ts`;
  `packages/shared/src/server/repositories/trace-sessions.ts` (or services). Sessions group traces
  by sessionId; list + detail; aggregate cost/latency across a session's traces. (Verify exact
  file + functions while authoring.)
- **L27 公共 REST API**: `web/src/features/public-api/server/createAuthedProjectAPIRoute.ts`
  (config :38, handler :300, `verifyAuth` :267, rate limit :368, Zod query/body :391/:413, OTel
  surface=publicapi, oversized → PayloadTooLargeError); `withMiddlewares.ts` (CORS + CH error);
  versioning by folder (v1 root, `v2/`, `v3/scores` cursor pagination); auth+cache
  `apiAuth.ts:75` (`ApiAuthService.verifyAuthHeaderAndReturnScope`, Redis key cache
  `fetchApiKeyAndAddToRedis` :311, negative caching); Fern `fern/apis/{server,client}/definition/
  *.yml` → `generated/{python,typescript}`.

## Lessons (titles from design spec)
- L20 web 应用架构（Next.js·Pages·三种 API）
- L21 tRPC 骨架（context·中间件·procedure）
- L22 仓储层：从 ClickHouse 读（executor·tags·retry·look-back 窗口）
- L23 过滤·搜索栏·查询构建（FilterState→SQL 两套栈·AI filter 往返校验）
- L24 列表与表格（compact vs metrics·conditional FINAL·预设·URL 状态）
- L25 trace 详情与观测树（full fetch·verbosity·agent graph）
- L26 sessions（按 sessionId 聚合 traces）
- L27 公共 REST API（路由工厂·版本·Fern 契约）

## Execution & audit (M4)
1. Create `src/part4.py`; author L20–L27; wire PAGES/SUBTITLES/CONTENT (registry imports part4) +
   quizzes (3 MCQ — opts close with `]` — + 1 open each). MIN_CJK=1800, MIN_SVGS=4.
2. Gates: build/build_print/check_html (0/0)/check_links; rebuild-diff clean; SVG bounds.
3. **Check**: source-fidelity per lesson (verify each citation in the real repo); a read-only audit
   subagent (opus-4.8, long-context) for the milestone. Log `2026-..-m4-audit.md`; roadmap M4 →
   done; commit per lesson.
