# M2 Audit — Part 2 · 前置基础 (L06–L11)

**Date:** 2026-06-27 · **Scope:** M2 (Part 2: L06–L11) · **Module:** `src/part2.py`
**Langfuse source audited:** `/home/verden/course/langfuse`

## Execution note
Opus file-writing subagents remain unusable here (the L09 author subagent ran 18 min / 0 files,
still "preparing to write"). Lessons authored directly by the main Opus 4.8 model. A **read-only
audit subagent** (`audit-m2`) was dispatched to independently verify source fidelity — its findings
are appended below; the orchestrator also verified the load-bearing claims directly (table below).

## Automated gates — PASS
- `build.py` + `build_print.py`: 11 lessons + index + print editions regenerate clean.
- `check_html.py`: **0 error / 0 warning** across all 11 lessons.
- `check_links.py`: all 44 internal links resolve.
- Rebuild-and-diff clean (committed HTML in sync).

## SVG-correctness — PASS
Coordinate-bounds check on L06–L11: **0 elements outside viewBox** (L06=4, L07=4, L08=6, L09=4,
L10=4, L11=4 SVGs). All use CSS variables; `role="img"`+`aria-label` present. (L02/L05 rendered in
both light & dark earlier; design system unchanged.)

## Content-completeness — PASS
Each of L06–L11: full arc (lead → analogy → ≥2 figures → cited-source body → key-points + spark
cards → 3-MCQ + 1-open quiz); bilingual parity; ≥2 `.fig` SVGs/lang; ≥3 HTML visual blocks/lang;
CJK ≥ 1800.

## Source-fidelity — orchestrator-verified (0 discrepancies)
| Lesson | Key claim | Verified against |
| --- | --- | --- |
| L06 | eventTypes TRACE_CREATE/OBSERVATION_*/SPAN_*/GENERATION_CREATE/SCORE_CREATE; OTLP + native ingest | `ingestion/types.ts` (via schemaUtils refs), `otel/v1/traces`, `ingestion.ts` exist |
| L07 | PG/CH/Redis/S3 clients | `db.ts`, `clickhouse/client.ts`, `redis/redis.ts`, `s3/index.ts` exist |
| L08 | ReplacingMergeTree(event_ts,is_deleted), toYYYYMM, ORDER BY project_id…, ZSTD(3), bloom_filter | `0002_observations.up.sql` (6 matches verbatim) |
| L09 | Legacy* models; ApiKey hashed fields; Role OWNER/ADMIN/MEMBER/VIEWER/NONE; onDelete:Cascade; @@map projects | `schema.prisma` (Legacy=3, hashed=8, Role=5, Cascade=84, projects=1) |
| L10 | environment LowCardinality DEFAULT 'default' AFTER project_id on 3 tables | `0008_add_environments_column.up.sql` (3 ALTERs verbatim) |
| L11 | web/worker images + clickhouse/redis:7/postgres/minio | `docker-compose.yml` (6 services verified) |

## Subagent audit findings (audit-m2)
> _Appended after the read-only audit subagent returns._

## Sign-off
M2 complete pending subagent confirmation. Part 2 (L06–L11) built, audited, source-grounded, all
gates green. Proceeding to M3 (Part 3 · 摄取链路, L12–L19).
