# M6 Audit — Parts 6+7 · 数据集/实验 + Prompt/Playground (L34–L39)

**Date:** 2026-06-27 · **Milestone:** M6 · **Modules:** `src/part6.py` (L34–36), `src/part7.py` (L37–39)
**Langfuse source audited:** `/home/verden/course/langfuse`
**Auditor:** read-only subagent (claude-opus-4.8, long-context, high reasoning), 23 explicit
claims verified against real source file:line; orchestrator authored + spot-checked.

## Automated gates — PASS
- `build.py` + `build_print.py`: all 39 lessons + index render; print_zh/print_en built.
- `check_html.py`: **0 error / 0 warning** across all 39 lessons (MIN_CJK=1800, MIN_SVGS=4,
  ≥6 visual blocks ≥3/lang, key-points + analogy cards, bilingual parity, nav chain).
- `check_links.py`: all 156 internal links resolve.
- Rebuild-and-diff clean (committed HTML in sync with sources).

## SVG-correctness — PASS
Coordinate-bounds check on built HTML for L34–L39: **0 elements outside viewBox**
(each lesson ≥4 SVGs ≥2/lang; L37/L38 have 6). All SVGs use CSS variables + `role="img"` + `aria-label`.

## Content-completeness — PASS
Each of L34–L39: full arc (lead → analogy → ≥2 `.fig` SVGs/lang → cited-source body → key-points +
spark cards → 3-MCQ + 1-open quiz); bilingual parity; ≥3 HTML visual blocks/lang; CJK ≥ 1800.

## Source-fidelity — subagent-verified (23/23 claims), 2 citation issues found + fixed
| Lesson | Key claim | Verified against |
| --- | --- | --- |
| L34 | Dataset/DatasetItem (input/expectedOutput/sourceTraceId/sourceObservationId, validFrom/validTo/isDeleted, @@id([id,projectId,validFrom])); DatasetStatus ACTIVE/ARCHIVED; VERSIONED SCD-2 write (close validTo + insert new); run pins validFrom (eval uses datasetItemValidFrom) | `schema.prisma:661,685,714`; `dataset-items.ts:418-470`; `evalService.ts:500-501,1220` |
| L35 | DatasetRuns unique[datasetId,projectId,name]; DatasetRunItems runId+itemId→traceId; CH dataset_run_items_rmt denormalizes run fields + snapshots item input/expectedOutput, ReplacingMergeTree(event_ts,is_deleted), ORDER BY project/dataset/run/id; dataset-run-item-upsert → createEvalJobs | `schema.prisma:719,739`; `clustered/0024_dataset_run_items.up.sql`; `evalQueue.ts:45-54` |
| L36 | createExperimentJobClickhouse:287; per-item replaceVariablesInPrompt + env=PromptExperiments + prompt link + itemVersion=validFrom + provider/model; experimentCreateQueueProcessor (ExperimentCreate); comparison UI (Baseline/Comparison/ChartsGrid); Dataset.remoteExperimentUrl | `experimentServiceClickhouse.ts:154-225,287`; `experimentQueue.ts:15`; `web/.../experiments/components/*`; `schema.prisma:667` |
| L37 | Prompt (version Int immutable, labels[], commitMessage, @@unique[projectId,name,version]); PromptDependency childLabel/childVersion; PromptProtectedLabels; createPrompt version+1 + LATEST label + removeLabelsFromPreviousPromptVersions; PRODUCTION/LATEST_PROMPT_LABEL | `schema.prisma:835,864,884`; `createPrompt.ts:88-181`; `constants.ts:1-2` |
| L38 | PromptService Redis read-through (getPrompt:49, cachePrompt SET EX ttlSeconds); resolvePrompt inlines deps (MAX_PROMPT_NESTING_DEPTH=5, seen cycle); epoch invalidation (rotate project-scoped token, key prompt:proj:epoch:name:ver/label, epochTtl 7d, randomBytes(6) base64url, SET NX) | `PromptService/index.ts:42-49,132-144,173,179-260` |
| L39 | Playground chatCompletionHandler reuses fetchLLMCompletion (tools+structuredOutputSchema); AES-256-GCM encrypt (random 12B IV, authTag, iv:enc:tag, ENCRYPTION_KEY 256-bit); LlmApiKeys (secretKey/displaySecretKey/adapter/baseURL); LlmSchema/LlmTool; decrypt at fetchLLMCompletion | `chatCompletionHandler.ts:19,78`; `encryption.ts:18-34`; `schema.prisma:299,1462,1478`; `fetchLLMCompletion.ts:55,369` |

## Issues found & fixed (2 — both citation/labeling, technical substance correct)
- **L35 — wrong file citation:** the run-level score-aggregation code-frame (`agg_scores_avg`,
  `scores_aggregated` CTE) was attributed to `dataset-runs.ts`, but those live in
  **`dataset-run-items.ts`** (field `:123`, CTE `:329`); only `createOrFetchDatasetRun` is in
  `dataset-runs.ts:30`. Fixed all 8 prose/codefile refs in part6.py + 4 quiz refs → `dataset-run-items.ts`.
- **L36 — wrong function name:** the code-frame "processItem core" + cited lines `:155-218` actually
  show the body of **`processLLMCall`** (`:154-225`); `processItem` (`:68-152`) delegates to it.
  Fixed codefile title + figcap to `processLLMCall (delegated by processItem)` + line range `:154-225`
  (zh+en) and the 2 quiz answers. Also added the omitted `adapter: config.validatedApiKey.adapter`
  to the shown `modelParams` snippet.
- Other **21/23 claims VERIFIED** against real source with correct file:line citations.

## Verdict — M6 PASS (after fixes)
Parts 6 & 7 (L34–L39) are source-faithful, gate-clean (39/39, 0/0), SVG-correct, bilingual-complete.
The datasets→runs→experiments regression-testing loop and the prompt git-model→serving-cache→playground
developer workflow are grounded; the SCD-2 item versioning, CH run-item snapshot, epoch cache
invalidation, and AES-256-GCM credential encryption are all verified against the real shared/worker/web code.
