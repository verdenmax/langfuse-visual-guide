# M3 Audit — Part 3 · 摄取链路 / The ingestion path (L12–L19)

**Date:** 2026-06-27 · **Milestone:** M3 · **Module:** `src/part3.py`
**Langfuse source audited:** `/home/verden/course/langfuse`

## Automated gates — PASS
- `build.py` + `build_print.py`: all 19 lessons + index render; print_zh/print_en built.
- `check_html.py`: **0 error / 0 warning** across all 19 lessons (MIN_CJK=1800, MIN_SVGS=4,
  ≥6 visual blocks ≥3/lang, key-points + analogy cards, bilingual parity, nav chain).
- `check_links.py`: all 76 internal links resolve.
- Rebuild-and-diff clean (committed HTML in sync with sources).

## SVG-correctness — PASS
Coordinate-bounds check on the built HTML for L12–L19: **0 elements outside viewBox**
(each lesson 4 SVGs = 2/lang). All SVGs use CSS variables + `role="img"` + `aria-label`.

## Content-completeness — PASS
Each of L12–L19: full arc (lead → analogy → ≥2 `.fig` SVGs/lang → cited-source body → key-points +
spark cards → 3-MCQ + 1-open quiz); bilingual parity; ≥3 HTML visual blocks/lang
(from `.cols`/`.vflow`/`.layers`/`table.t`); CJK ≥ 1800.

## Source-fidelity — orchestrator-verified (citations checked while authoring)
| Lesson | Key claim | Verified against |
| --- | --- | --- |
| L12 | handler → processEventBatch; `res.status(207).json(result)` with `{successes,errors}`; reused by scores/events/generations/spans/traces | `web/src/pages/api/public/ingestion.ts:23,158,162`; `processEventBatch.ts:113` |
| L13 | `base{id,timestamp,metadata}` + `discriminatedUnion("type",…)` (18 types); body chain Optional→Event(+id)→Span(+endTime)→Generation(+model/usage); 7 new types reuse CreateGenerationBody; `getClickhouseEntityType`→trace/observation/score; ObservationType enum = 10 values | `ingestion/types.ts:259-279,395-470,597-719`; `clickhouse/schemaUtils.ts:17`; `prisma/schema.prisma:485-498` |
| L14 | Redis+BullMQ; payload carries `fileKey` pointer (body in S3); shardingKey `projectId-eventBodyId`, SHA-256%N; secondary redirect by env allowlist OR S3 slowdown flag; attempts:6 backoff; getDelay ~5s + UTC date-boundary | `queues.ts:364-365`; `processEventBatch.ts:340-398,65-91`; `redis/sharding.ts:9`; `redis/ingestionQueue.ts:14-72`; `worker/.../ingestionQueue.ts:109-134` |
| L15 | `mergeAndWrite` switch→process*EventList; `mergeRecords` left-fold + `event_ts=now`; `overwriteObject` 3 skip-rules; immutableEntityKeys[Observations]; CH record first in merge; worker reads all S3 events via listFiles + idempotency skip; wrapper-trace backfill; addToQueue (not direct write) | `IngestionService/index.ts:149-195,738-897,947-1005,107-114,853-872`; `IngestionService/utils.ts:56-81`; `worker/.../ingestionQueue.ts:182` |
| L16 | getGenerationUsage 4 steps; tokenize only if model+empty-provided+non-ERROR via tokenizerId; provided-cost disables computation; findModel regex `~ match_pattern`, project>global, latest start_date; real gpt-4o pattern `(?i)^(openai/)?(gpt-4o)$` | `IngestionService/index.ts:1057-1240`; `calculateUsageCosts`; `ingestion/modelMatch.ts:269-305`; `constants/default-model-prices.json` |
| L17 | singleton; per-table queues; flush on batchSize (addToQueue) OR writeInterval (setInterval→flushAll); flush splices+writeToClickhouse(JSONEachRow) backoff; shutdown flushAll(true) drain | `ClickhouseWriter/index.ts:34-135,358-398,550-600` |
| L18 | OTLP endpoint parses protobuf/JSON+gzip→resourceSpans→publishToOtelIngestionQueue (S3+queue); OtelIngestionProcessor extractModelName priority list (gen_ai/ai/llm/native); input/output from span events; processToIngestionEvents→native events→split observation(IngestionService)/trace(processEventBatch) | `web/.../otel/v1/traces/index.ts`; `otel/OtelIngestionProcessor.ts:182,2232-2257`; `worker/.../otelIngestionQueue.ts:300-360`; `queues.ts:362-363` |
| L19 | `MEDIA_REFERENCE_PATTERN=/@@@langfuseMedia:.+?@@@/g`; blob_storage_file_log ReplacingMergeTree wide table; presigned upload + sha256 dedup (uploadUrl:null on hit); separate S3 media bucket; GET media download URL | `utils/mediaReferences.ts:6`; `clickhouse/migrations/.../0011_*.up.sql`; `media/server/mediaService.ts:35-122`; `media/[mediaId].ts` |

## Subagent audit findings (audit-m3)
Read-only `general-purpose` audit subagent (opus-4.8, long-context, ~365s) independently re-verified
every cited claim in L12–L19 against the real repo. Result: **No CRITICAL discrepancies — nothing
fabricated or factually broken; the guide is highly faithful.** One real factual inaccuracy (L13,
fixed) + two cosmetic notes (no change needed):

| # | Lesson | Finding | Action |
| --- | --- | --- | --- |
| 1 | L13 | Said `getClickhouseEntityType` collapses to "**3** entity types". Actual (`schemaUtils.ts:10-50`): **5** entity types — `trace · observation · score · sdk_log · dataset_run_item`. The "observation-like → observation" claim and the 3-core→3-wide-tables mapping are correct, but the count was understated. | **FIXED** (zh+en+quiz): reworded to "5 entity types, of which the 3 core ones (trace/observation/score) map to L08's 3 wide tables; sdk_log & dataset_run_item handled separately"; added 2 table rows. Re-verified CJK 1963, 0/0. |
| 2 | L16 | Cosmetic: the cited SQL lives in `findModelInPostgres` (wrapped by `findModel`); the line range 269-305 is correct. | No change — citation accurate. |
| 3 | L19 | Cosmetic: the `LANGFUSE_S3_MEDIA_MAX_CONTENT_LENGTH` cap is enforced in the route handler `media/index.ts:40-42`, not inside `mediaService.ts`; the cap itself is real. | No change — cap real, figcap cites mediaService for the dedup/presigned flow which is correct. |

All other claims across L12–L19 (envelope/discriminatedUnion, two-id merge, sharding, secondary
queue, mergeRecords/overwriteObject, immutable keys, provided-beats-computed, findModel regex,
ClickhouseWriter triggers, OTel adapter priority lists, media reference/dedup/blob_storage_file_log)
checked out **exactly as cited**.

## Sign-off
M3 complete and **subagent-confirmed**. The read-only audit found 1 genuine source-fidelity bug
(L13 entity-type count), now corrected, plus 2 cosmetic notes. Part 3 (L12–L19) built, audited,
source-grounded, all gates green (19/19 lessons, 76 links, 0/0). Proceeding to M4 (Part 4 · 查询链路,
L20–L27).
