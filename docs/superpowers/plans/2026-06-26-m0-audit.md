# M0 Audit — Infrastructure & design system + L01

**Date:** 2026-06-26 · **Scope:** M0 (scaffold + retarget design system + L01 baseline)
**Langfuse source audited:** `/home/verden/course/langfuse` (langfuse/langfuse)

## Automated gates — PASS
- `python3 build.py` → wrote `index.html` + `lessons/01-what-is-langfuse.html`.
- `python3 build_print.py` → wrote `print_zh.html`, `print_en.html`.
- `python3 check_html.py` → **0 error / 0 warning**, structural check passed.
- `python3 check_links.py` → all 4 internal links resolve.
- New `MIN_SVGS=4` gate active; L01 has 6 SVGs (3 per language).

## Content-completeness — PASS
L01 has the full arc: lead → analogy card → macro figure → 4 body `<h2>` sections (what it is /
three pillars / what it's made of / why this design) + worked-example debugging scene + how-to-read
+ key-points card → 3-MCQ + 1-open self-test. Bilingual zh/en parity. Floors met: zh CJK ≈ 3000+,
6 SVGs, 5 HTML visual blocks per language, key-points + analogy cards present.

## SVG-correctness — PASS
- Coordinate-bounds check: 0 elements outside their `viewBox` across all 6 SVGs.
- All fills/strokes use CSS variables (theme in light + dark); `role="img"` + `aria-label` present.
- Headless Chromium render of the full page reviewed: top bar, hero, all cards, 3 figures (macro
  loop / nested trace tree / system architecture), flow, vflow, layers, table and quiz all render
  correctly with consistent emerald theming.

## Source-fidelity — PASS (0 discrepancies)
Every technical claim in L01 verified against the real source:

| Claim in L01 | Verified against |
| --- | --- |
| develop → monitor → evaluate → debug loop | `README.md:70` (verbatim Langfuse self-description) |
| trace/observation/score domain models | `packages/shared/src/domain/{traces,observations,scores}.ts` exist |
| observation types GENERATION/SPAN/EVENT | `packages/shared/prisma/schema.prisma` `LegacyPrismaObservationType` |
| ScoreConfig in Postgres | `schema.prisma:550` `model ScoreConfig` |
| observations = ReplacingMergeTree, ZSTD, Map(usage/cost), prompt_id, monthly partition | `clickhouse/migrations/unclustered/0002_observations.up.sql` |
| 8 cited `web/src/features/*` dirs | all exist (traces, tracing-tables, evals, scores, annotation-queues, prompts, datasets, experiments) |
| pnpm + Turbo monorepo, docker-compose | `pnpm-workspace.yaml`, `turbo.json`, `docker-compose.yml` exist |
| dependency direction web/worker/ee → shared; shared → none | `.agents/AGENTS.md:46-49` |
| async ingestion via Redis queue (S3-merge) | `ingestion.ts`, `ingestionQueue.ts`, `queues.ts:364` "Process single events with S3-merge" |
| MIT license | `LICENSE` |

## Sign-off
M0 complete. Infrastructure retargeted to Langfuse (emerald theme, `lvg*` toggle, MIN_SVGS gate),
L01 ships as the gold-standard visual baseline, all gates green, all claims grounded. Ready to
commit and proceed to M1 (Part 1, L02–L05).
