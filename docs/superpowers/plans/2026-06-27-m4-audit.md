# M4 Audit — Part 4 · 查询链路 / The read path (L20–L27)

**Date:** 2026-06-27 · **Milestone:** M4 · **Module:** `src/part4.py`
**Langfuse source audited:** `/home/verden/course/langfuse`

## Automated gates — PASS
- `build.py` + `build_print.py`: all 27 lessons + index render; print_zh/print_en built.
- `check_html.py`: **0 error / 0 warning** across all 27 lessons (MIN_CJK=1800, MIN_SVGS=4,
  ≥6 visual blocks ≥3/lang, key-points + analogy cards, bilingual parity, nav chain).
- `check_links.py`: all 108 internal links resolve.
- Rebuild-and-diff clean (committed HTML in sync with sources).

## SVG-correctness — PASS
Coordinate-bounds check on the built HTML for L20–L27: **0 elements outside viewBox**
(each lesson 4 SVGs = 2/lang). All SVGs use CSS variables + `role="img"` + `aria-label`.

## Content-completeness — PASS
Each of L20–L27: full arc (lead → analogy → ≥2 `.fig` SVGs/lang → cited-source body → key-points +
spark cards → 3-MCQ + 1-open quiz); bilingual parity; ≥3 HTML visual blocks/lang; CJK ≥ 1800.

## Source-fidelity — orchestrator-verified (citations checked while authoring)
| Lesson | Key claim | Verified against |
| --- | --- | --- |
| L20 | Pages Router primary; App Router exactly 4 files; three API styles (tRPC/REST/App-Router-SSE); features/~80 | `web/src/app/` (layout + 3 route.ts); `pages/api/trpc/[trpc].ts`; `pages/api/public/**`; `pages/api/dashboard/execute-query-stream.ts` |
| L21 | appRouter ~64 routers; createTRPCContext {session,headers,prisma}; procedure middleware stacks; enforceUserIsAuthedAndProjectMember parses projectId from getRawInput | `server/api/root.ts:70`; `server/api/trpc.ts:57,175,218,249,272,288,379,464,483` |
| L22 | queryClickhouse OTel+tags(log_comment)+backoff+resource-wrap; streaming variants; look-back windows 2d/1h | `repositories/clickhouse.ts:580,600,639,249,705`; `repositories/constants.ts:4,9` |
| L23 | FilterState→CH/PG SQL; apply() random param names; factory column/type verify + project_id; search-bar single-source-of-truth; AI filter round-trip | `clickhouse-sql/clickhouse-filter.ts:20,36,62`; `factory.ts:37,217`; `filterToPrisma.ts:26`; `search-bar/README.md`; `search-bar/server/router.ts:45` |
| L24 | getTracesTableGeneric 4 shapes; conditional joins; conditional FINAL (toDate,event_ts DESC + LIMIT 1 BY id vs FINAL); joinTableCoreAndMetrics merge-by-id; URL/session/DB presets | `traces-ui-table-service.ts:164,206,271,276,443-455`; `components/table/utils/joinTableCoreAndMetrics.ts:1` |
| L25 | byIdWithObservationsAndScores parallel tree(includeIO:false)+scores; partition CORRECTION; derive latency; observation IO stripped; verbosity; getAgentGraphData | `routers/traces.ts:348,355,368,386,390,405,438-442,610`; `trpc.ts:480,524-525` |
| L26 | session = GROUP BY session_id over traces (derived view); getSessionsTableGeneric mirrors L24; user_ids/duration/session_total_cost aggregates | `services/sessions-ui-table-service.ts:86,123,132-160` |
| L27 | route factory auth/access/rate-limit/Zod/OTel/PayloadTooLarge; apiAuth Redis cache + negative caching; folder versions v2/v3; Fern → SDKs | `public-api/server/createAuthedProjectAPIRoute.ts`; `public-api/server/apiAuth.ts:75`; `pages/api/public/v2,v3/`; `fern/apis/` |

## Subagent audit findings (audit-m4)
Read-only `general-purpose` audit subagent (opus-4.8, long-context, ~293s) independently re-verified
every cited claim in L20–L27 against the real repo. Result: **1 CRITICAL discrepancy (L26, fixed)
+ 3 MINOR imprecisions (fixed)**; everything else checks out precisely against the cited file:line.

| # | Lesson | Finding | Action |
| --- | --- | --- | --- |
| 1 | L26 | **CRITICAL over-claim**: "a session is a derived view, **not a separately stored entity / no new table / zero extra writes**". Wrong — a Postgres `TraceSession` table (`schema.prisma:387`, `@@map("trace_sessions")`) **is upserted on ingestion** (`IngestionService/index.ts:678`, `INSERT … ON CONFLICT DO NOTHING`) whenever a trace has a session_id. The accurate nuance: a **lightweight metadata stub** (id/project/environment + bookmarked/public) is persisted, but all **metrics/listing are derived** by GROUP BY over ClickHouse traces. | **FIXED** (zh+en lead/GROUP-BY/cols/spark/key + both quizzes): reframed to "metrics & listing are derived; a tiny `trace_sessions` metadata stub is persisted on ingestion"; added an explicit clarifying paragraph; reworded the cols to "compute-on-the-fly vs pre-stored aggregates". |
| 2 | L26 | MINOR: sessions service has **3** select shapes (count/rows/metrics), not the "four shapes" of traces (which adds `identifiers`). | **FIXED**: changed "四种形状/four shapes" → "count/rows/metrics (one fewer than traces, no identifiers)" in zh+en. |
| 3 | L22 | MINOR: the "~98% within 5 min" figure is **not** in `repositories/README.md`; the real figure is **96% of observations start within 2 min** of the trace (and 97% of traces within 2 min). | **FIXED** (zh+en): changed to "~96% within 2 minutes". |
| 4 | L20 | MINOR: `execute-query-stream.ts` is in `pages/api/dashboard/` = **Pages Router**, so labeling it "App-Router/SSE" conflates it with the 4 App Router files. Also `features/` is **68** dirs, not "~80". | **FIXED** (zh+en): relabeled the third door "special-semantics (App Router handlers + a Pages-Router streaming SSE endpoint)"; changed "~80" → "dozens". |

Soft notes (no change): appRouter aggregates 62 routers (guide "~64" fine as approximation);
L24 default clause is `LIMIT 1 BY id, project_id` (guide said "id" — trivial). All other claims
across L20–L27 (procedure middleware stacks, queryClickhouse wrapping, FilterState→SQL injection
safety, AI-filter round-trip, conditional FINAL/JOIN, trace-detail includeIO:false + verbosity,
route factory + Redis/negative-cache auth + folder versions + Fern) verified **exactly as cited**.

All fixes re-verified: `check_html` 0/0, `check_links` 108 ok, CJK still ≥1800 (L26 now 2162),
rebuild in sync.

## Sign-off
M4 complete and **subagent-confirmed**. The read-only audit caught 1 genuine source-fidelity bug
(L26 session-storage over-claim) + 3 minor imprecisions — all corrected. Part 4 (L20–L27) built,
audited, source-grounded, all gates green (27/27 lessons, 108 links, 0/0). Proceeding to M5
(Part 5 · 评估与评分, L28–L33).
