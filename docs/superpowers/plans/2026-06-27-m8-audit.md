# M8 审计日志 — Part 10 · 平台与运维 (L48–L53)

**日期:** 2026-06-27 · **审计方式:** 只读子代理（claude-opus-4.8 · long_context · high reasoning）

## 范围
M8（Part 10 平台与运维）共 6 课，本会话全部新撰写并提交。本审计对每课的关键技术论断
（含 path:line 引用）逐条核验是否忠实于 `/home/verden/course/langfuse` 真实源码。

- L48 鉴权与会话（NextAuth + 密码安全护栏 + JWT 会话注入身份）
- L49 RBAC·API key·SCIM（两类授权对象 + 两层哈希 + 纵深防御）
- L50 开源核与权益（open-core + entitlement 门控 + EE 许可证）
- L51 自我可观测与配置（dogfooding OTel + 日志关联 trace + Zod env 校验）
- L52 数据生命周期与删除（跨存储删除 + 删除顺序 + 后台迁移）
- L53 构建·测试·开发流（pnpm monorepo + Turbo 管道/缓存 + 一键 dx）

## 方法
向只读子代理给出 **36 条带 file:line 的具体论断**（L48:9 / L49:6 / L50:7 / L51:5 / L52:7 /
L53:6），要求逐条打开真实源码核验，分类 ✅ VERIFIED / ⚠️ MINOR / ❌ WRONG，并对任何
⚠️/❌ 给出正确事实与行号；另要求对 6 个构建产物 HTML 做「伪造标识符」全量扫描。

## 结果：36/36 全部 ✅ VERIFIED（0 ⚠️ MINOR · 0 ❌ WRONG）

子代理逐条核验通过，**无需任何修正**。重点行为论断全部属实：

- **L48**：`auth.ts:743/744` strategy:"jwt" + maxAge；`:82/87/97` SSO 强制三闸；`:108-111`
  时序攻击防护（用户不存在也跑 hashPassword + 注释）；`:114-117` password=null 引导 IdP；
  `credentialsServerUtils.ts:65-66/70-71` bcrypt hash(12)/compare；`:748` session 回调被
  `instrumentAsync("next-auth-session")` 包裹、注入 orgMemberships→projects+ProjectMemberships；
  `:592/594` extendedPrismaAdapter；`:739` static+dynamic SSO 合并。
- **L49**：`projectAccessRights.ts:5/91` projectScopes + roleAccessRights（OWNER:92/ADMIN:150/
  MEMBER:207/VIEWER:251 仅 read/NONE:271 空）；`checkProjectAccess.ts:28/42` throwIfNoProjectAccess
  (FORBIDDEN)+useHasProjectAccess；`apiKeys.ts:13/19/31/41/52/73-75` generateKeySet/bcrypt/
  createShaHash/三态存储/salt；`apiAuth.ts:75/97/330/129-144` 快哈希查 + 旧 key bcrypt 验后升级 +
  API_KEY_NON_EXISTENT 负缓存；SCIM 端点齐全；两层哈希设计原理（sha256 确定性可索引 / bcrypt 加盐不可索引）属实。
- **L50**：`entitlements.ts:6/38/54` entitlements/limits/entitlementAccess；具体额度
  hobby(member 2/days 30)/core(false/90)/pro(+data-retention) 数字精确；limit 值 number|false；
  `hasEntitlement.ts:18/25/40` admin→true / plan??"oss" / includes；`plans.ts:21/26/27`
  Plan+isCloud/SelfHosted；`licenseCheck/index.ts:15-22` Cloud 或 langfuse_ee_（pro 不算）；
  依赖方向 web/worker/ee→shared（AGENTS.md:46-49）。
- **L51**：`instrumentation/index.ts:54/140/142/263/328/343/354` instrumentAsync + 同模块各函数；
  `logger.ts:6/31/57` tracingFormat 注入 trace_id/span_id+dd.* / text|json by env / level by env；
  OtelIngestionProcessor 存在；`env.ts:15` EnvSchema z.object（REDIS_PORT coerce/TLS enum）、
  `env.mjs:40` createEnv server/client；instrumentAsync 确在 eval/monitor/集成/auth 复用。
- **L52**：`dataRetentionQueue.ts:11/23` 两级 fan-out（EE 目录）；`handleDataRetentionProcessingJob.ts:30/58-59/89-98`
  retentionDays/cutoffDate/跨 S3+CH 清理；`projectDelete.ts:22/61/62-75/97` 注释 "ClickHouse first"、
  Promise.all 清 CH+S3、PG 最后删；`traceDelete.ts:15`；`schema.prisma:284` BackgroundMigration
  全字段（state/lockedAt/workerId/finishedAt/failedReason）；`backgroundMigrationManager.ts:6/17/38/42/127`
  + IBackgroundMigration validate/run/abort；三存储跨删确认。
- **L53**：`turbo.json:12-14/38-41/53-57` build(dependsOn ^build/outputs/cache:true)、dev(cache:false
  persistent)、db:generate(cache:false+node_modules 注释)；`pnpm-workspace.yaml:3-8` packages+minimumReleaseAge:7200；
  `package.json:102/14/34/24/23` pnpm@11.4.0 / only-allow / dev:web filter / dx 链 / seed；seeder 目录齐全；
  依赖方向确认。

**伪造标识符扫描**：从 part10.py 提取所有文件名与相关 camelCase 标识符，**全部解析到真实符号**
（含 hasEntitlementLimit、deleteDatasetRunItemsByProjectId、getOrganizationPlanServerSide、
removeIngestionEventsFromS3AndDeleteClickhouseRefsForProject 等），**未发现伪造**。

非问题精度提示（准确、无需改动）：L50 Plan 还含 cloud:team（课中「+…」为示意非穷举）；
L53 preinstall 精确值为 `npx only-allow pnpm`（课中「only-allow pnpm」同机制）；seed:scenario 经 cli.ts 包 cli-main.ts（二者皆存在）。

## 结论
M8（L48–L53）全部 6 课完成，经只读审计 **零真错、零小错**，源码忠实度满分。所有 gate
（build / build_print / check_html 0/0 / check_links / SVG 坐标）全绿，每课一 commit。
M8 标记为 ☑ done。下一步 M9（Part 11 设计专题与终章 L54–L55），完成后即全 55 课收官。
