# M5 Audit — Part 5 · 评估与评分 / Evaluation & scoring (L28–L33)

**Date:** 2026-06-27 · **Milestone:** M5 · **Module:** `src/part5.py`
**Langfuse source audited:** `/home/verden/course/langfuse`
**Auditor:** read-only subagent (claude-opus-4.8, long-context, high reasoning), 34 explicit
claims verified against real source file:line; orchestrator authored + spot-checked.

## Automated gates — PASS
- `build.py` + `build_print.py`: all 33 lessons + index render; print_zh/print_en built.
- `check_html.py`: **0 error / 0 warning** across all 33 lessons (MIN_CJK=1800, MIN_SVGS=4,
  ≥6 visual blocks ≥3/lang, key-points + analogy cards, bilingual parity, nav chain).
- `check_links.py`: all 132 internal links resolve.
- Rebuild-and-diff clean (committed HTML in sync with sources).

## SVG-correctness — PASS
Coordinate-bounds check on the built HTML for L28–L33: **0 elements outside viewBox**
(each lesson ≥4 SVGs ≥2/lang; L33 has 6). All SVGs use CSS variables + `role="img"` + `aria-label`.

## Content-completeness — PASS
Each of L28–L33: full arc (lead → analogy → ≥2 `.fig` SVGs/lang → cited-source body → key-points +
spark cards → 3-MCQ + 1-open quiz); bilingual parity; ≥3 HTML visual blocks/lang; CJK ≥ 1800.

## Source-fidelity — subagent-verified (34/34 claims), 1 real issue found + fixed
| Lesson | Key claim | Verified against |
| --- | --- | --- |
| L28 | 3 config dataTypes NUMERIC/CATEGORICAL/BOOLEAN; CATEGORICAL labels+values both unique; BOOLEAN `.length(2)` locked True=1/False=0; config is a 4-arm discriminatedUnion (+TEXT, no CORRECTION) | `packages/shared/src/domain/score-configs.ts:18-80,117-138` |
| L28 | ScoreSourceArray=[API,EVAL,ANNOTATION]; ScoreDataTypeArray has 5 (incl. CORRECTION/TEXT); value always number, stringValue=label; TEXT≤500, CORRECTION name="output"; score refs one-of trace/session/datasetRun/observation | `packages/shared/src/domain/scores.ts:4,42-52,89-133` |
| L29 | runLLMAsJudgeEvaluation:735; compileEvalPrompt:784; callLLM structuredOutputSchema; env=LLMJudge; toNormalizedScores comment=reasoning, NUMERIC value=score / BOOLEAN value=score?1:0 / CATEGORICAL matches.map; source=EVAL SCORE_CREATE re-enters ingestion | `worker/src/features/evaluation/evalService.ts:180,735,884-894,963`; `evalRuntime.ts:14`; `evalScoreEvent.ts:52` |
| L30 | createEvalJobs:180 fan-out ACTIVE configs; langfuse- loop guard:243; InMemoryFilterService.evaluateFilter:422; 3 gates dedup/sampling(config.sampling)/delay(config.delay); JobExecution PENDING + enqueue EvaluationExecution; deselect→CANCELLED; status enum 5; evalCompletion COMPLETED+jobOutputScoreId+enqueueScoreIngestion; 2-stage queues + DELAYED ~120min | `evalService.ts:180,243,422,612-700`; `schema.prisma:1080,1088`; `evalCompletion.ts:64,83-92`; `worker/src/queues/evalQueue.ts:25,46,98,126,151,197-230` |
| L31 | size limits 256KB/5.5MB/256KB; CodeEvalDispatcher {name,dispatch}→DispatchResult{scores≥1}; Lambda name="aws-lambda" per-language python/node; local name="insecure-local" node vm TS/JS only; TIMEOUT msg "network calls forbidden"; same source=EVAL flow-back | `codeEvalDispatcherTypes.ts:4-6,111-121`; `codeEvalDispatchers.ts:13,22`; `awsLambdaCodeEvalDispatcher.ts:23-25,146,182`; `localCodeEvalDispatcher.ts:1-2,17,28,43,77`; `codeEvalExecution.ts:44`; `executeCodeBasedEvaluation.ts:32,60,85` |
| L32 | AnnotationQueue scoreConfigIds; Item objectType TRACE/OBSERVATION/SESSION + status PENDING/COMPLETED + lockedAt/lockedByUserId/annotatorUserId; AnnotationQueueAssignment; 5-min soft lock isItemLocked; complete→COMPLETED+annotatorUserId; source=ANNOTATION + authorUserId | `schema.prisma:582-637`; `annotationQueueItemsRouter.ts:32-36,441-443`; `useScoreMutations.ts:39`; `comments.ts:97` |
| L33 | Monitor view/filters/metric "mirrors DashboardWidget" + window/cadence/threshold; operators GT/GTE/LT/LTE/EQ/NEQ; severity 6-state; computeSeverity null→noData mode, else alert→warning→OK; applyStateMachine PAUSED no-op + emit on change/renotify; monitorQueue publishes to WebhookQueue | `schema.prisma:1721-1752`; `processor/computeSeverity.ts`; `processor/applyStateMachine.ts:21-46`; `worker/src/queues/monitorQueue.ts:28-36` |

## Issue found & fixed (1)
- **L31 code excerpt (real bug):** the simplified `resolveConfiguredCodeEvalDispatcher` snippet
  selected the local dispatcher with `dispatcher === "local"`, but the real valid env value is
  **`"insecure-local"`** (`codeEvalDispatchers.ts:22`; `CodeEvalDispatcherName =
  "insecure-local" | "aws-lambda"`, `codeEvalDispatcherTypes.ts:164`). A reader copying the line
  would set the wrong `LANGFUSE_CODE_EVAL_DISPATCHER`. Fixed both zh+en code excerpts to
  `dispatcher === "insecure-local"`. (Prose, SVGs, and quizzes already used the correct name.)
- Other **33/34 claims VERIFIED** against real source with correct file:line citations.

## Verdict — M5 PASS (after fix)
Part 5 (L28–L33) is source-faithful, gate-clean (33/33, 0/0), SVG-correct, bilingual-complete.
The eval-domain through-line (score model → LLM-judge → execution pipeline → code eval → human
annotation → monitors) is grounded; all three score sources (API/EVAL/ANNOTATION) and the
eval→SCORE_CREATE→ingestion re-entry are verified against the real worker/shared code.
